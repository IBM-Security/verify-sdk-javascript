// let OAuthContext = require('../../../dist').OAuthContext;
// let expect       = require('chai').expect;
// let config       = require("./helper").config;

// let authClient;

// describe("OAuthContext - token()", () => {
//     before(() => {
//         authClient = new OAuthContext(config);
//     })

//     describe("token()", () => {
//         it("to do", () => {
//             expect(1).to.equal(2);
//         })
//     })
// })

